PreTeXt no tiene el `<strong>` de HTML porque eso indica formato y no estructura. Pero sí tiene un tag dos tags, con significado, que sirven en dos casos que uno usa negrillas:
*  `<term>` para definir términos.
* `<alert>`, como alerta para el lector

Se debe usar el tag correcto de acuerdo al significado.
