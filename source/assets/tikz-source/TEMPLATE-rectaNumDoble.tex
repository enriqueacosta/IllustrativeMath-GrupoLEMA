% EAJ - svg generation needs to be run with -shell-escape:
% pdflatex -shell-escape filename.tex
\documentclass[grado6]{LEMA-Tikz-IM}
\begin{document}
\begin{tikzpicture}

% Parameters
\def\lineWide{3in} % width of line (not including label)
\def\numMarks{5}
\def\topMarkLabels{0,300,...,1200} 
\def\topLineLabel{calcio (mg)}
\def\bottomMarkLabels{0\%, 25\%, p0\%, l5\%, 100\%} 
\def\bottomLineLabel{}

% doubleNumberLine creates several coordinates like (top-leftEnd) and (bot-mark-2). 
% See details in LEMA-Tikz-IM.cls
\doubleNumberLine{\lineWide}{\numMarks}{\topMarkLabels}{\topLineLabel}{\bottomMarkLabels}{\bottomLineLabel}

% Extra text in marks if needed
\node[tickLabel, below] at (top-mark-3) {text below};

% box around some top and bottom marks
\node[fit=(top-mark-2) (bot-mark-2), draw, rectangle, rounded corners=1.5ex, inner sep=3ex, very thick] (roundedRect) {};

\end{tikzpicture}
\end{document}